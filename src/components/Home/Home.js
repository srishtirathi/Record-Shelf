import React from 'react';
import './Home.css';

const Home = () => (
  <div className="home">
    <h4>Seems a bit empty in here :(</h4>
    <button type="button">Sync</button>
  </div>
);
export default Home;
